�，以避免覆盖他人的最新变更。</div>
      <div style="background:#fff7ed;border:1px solid #fed7aa;border-radius:10px;padding:12px 14px;margin-bottom:16px"><b>建议顺序：</b>先导出当前浏览器的脱敏本地副本，再重新载入云端最新版本。导出的副本会递归移除登录账号、密码、2FA、恢复密钥等敏感字段。</div>
      <div data-p1-revision style="font-size:12px;color:#64748b;margin-bottom:14px"></div>
      <div style="display:flex;gap:10px;flex-wrap:wrap">
        <button type="button" data-p1-backup style="border:1px solid #cbd5e1;border-radius:9px;background:white;padding:9px 13px;font-weight:700;cursor:pointer">导出本地脱敏副本</button>
        <button type="button" data-p1-reload style="border:0;border-radius:9px;background:#0f172a;color:white;padding:9px 13px;font-weight:700;cursor:pointer">重新载入云端最新版本</button>
        <button type="button" data-p1-later style="border:0;background:transparent;padding:9px 8px;color:#64748b;cursor:pointer">继续查看本页</button>
      </div>
      <div data-p1-status style="min-height:20px;margin-top:10px;font-size:12px;color:#64748b"></div>`;
    overlay.appendChild(card);
    document.body.appendChild(overlay);
    const rev=card.querySelector('[data-p1-revision]');
    if(rev)rev.textContent=state.remoteRevision!=null?`检测到的云端 revision：${state.remoteRevision}`:`冲突发生时间：${state.firstSeenAt||state.lastSeenAt||nowIso()}`;
    card.querySelector('[data-p1-backup]')?.addEventListener('click',()=>{
      const out=exportConfl